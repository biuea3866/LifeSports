let mongoose,Schema,bcrypt,jwt;_9fb‍.x([["default",()=>_9fb‍.o]]);_9fb‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}],["Schema",["Schema"],function(v){Schema=v}]]);_9fb‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_9fb‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);



/**
 *  2020 - 09 - 16 (Wed)
 *  Writer: 조봉준
 *  
 *  [ UserSchema Attribute ]  
 *  
 *  userId : userId that is unique
 *  hashedPassword : Entered password and transform to hash password
 *  userName: Real userName
 *  birth: 6 letters number of year, month, day
 *  gender: 1 letter to indicate gender 
 *                      e.g) 1, 3 - male / 2, 4 - female
 *  addr: User's address
 *  phoneMiddleNumber: Phone middle Number
 *  phoneLastNumber: Phone last Number
 */

const UserSchema = new Schema({
    email: String,
    hashedPassword: String,
    userName: String,                         // 본명
    nickName: String,
    socialNumberPrefix: String,
    socialNumberSuffix: String,
    addr: String,
    phoneNumber: String,
    point: Number,
});

/**
 *  2020 - 09 - 16 (Wed)
 *  Writer: 조봉준
 *  
 *  [ setPassword method ]  
 *  
 *  1. User enters password
 *  2. Convert this password using hash
 */

UserSchema.methods.setPassword = async function(password) {
    const hash = await bcrypt.hash(password, 10);

    this.hashedPassword = hash;
};

/**
 *  2020 - 09 - 16 (Wed)
 *  Writer: 조봉준
 *  
 *  [ checkPassword method ]  
 *  
 *  1. Compare password
 *  2. return true or false
 */

UserSchema.methods.checkPassword = async function(password) {
    const result = await bcrypt.compare(password, this.hashedPassword);

    return result;
};

/**
 *  2020 - 09 - 16 (Wed)
 *  Writer: 조봉준
 *  
 *  [ findByUserId method ]  
 *  
 *  1. Using userID find user information.
 */
UserSchema.statics.findByUserId = function(email) {
    return this.findOne({ email });
};

/**
 *  2020 - 09 - 16 (Wed)
 *  Writer: 조봉준
 *  
 *  [ serialize method ]  
 *  
 *  When user's data is responsed, to hide password field function
 */
UserSchema.methods.serialize = function() {
    const data = this.toJSON();

    delete data.hashedPassword;

    return data;
}

/**
 *  2020 - 09 - 17 (Thur)
 *  Writer: 조봉준
 *  
 *  [ generateToken method ]  
 *  
 *  For user's security, Tokens are delivered to users 
 *  when login and registration are successful
 */
UserSchema.methods.generateToken = function() {
    const token = jwt.sign(
        {
            _id: this.id,
            email: this.email,
        },

        process.env.JWT_SECRET,

        {
            expiresIn: '7d',
        },
    );

    return token;
};

const User = mongoose.model('User', UserSchema);

_9fb‍.d(User);