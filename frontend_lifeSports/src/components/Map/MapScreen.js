import * as React from 'react';
import {
  View,
  Text
} from 'react-native';

const MapScreen = () => {
  return(
    <View style={
      {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center'
      }
    }>
      <Text>Map Screen</Text>
    </View>
  );
}

export default MapScreen;