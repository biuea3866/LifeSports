let Rental;_6ab‍.x([["write",()=>write],["list",()=>list]]);_6ab‍.w("../../models/rental",[["default",["Rental"],function(v){Rental=v}]]);

       const write = async ctx => {
    const { 
        paymentType,
        amount,
        userName,
        tel,
        email,
        userId,
        date,
        time,
        mapId,
        deleteYn
    } = ctx.request.body;

    const rental = new Rental({
        paymentType,
        amount,
        userName,
        tel,
        email,
        userId,
        date,
        time,
        mapId,
        deleteYn
    });

    try {
        await rental.save();

        ctx.body = rental;
    } catch(e) {
        ctx.throw(500, e);
    }
};


       const list = async ctx => {
    const { userId } = ctx.params;

    try {
        const rentals = 
        await Rental.where("userId").equals(userId).exec();

        ctx.body = rentals;
    } catch(e) {
        ctx.throw(500, e);
    }
};
