let jwt;_b87‍.x([["default",()=>_b87‍.o]]);_b87‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

const jwtMiddleware = (ctx, next) => {
    const token = ctx.cookies.get('access_token');

    if( !token ) {
        return next();
    }

    try {
        const decoded = jwt.verify(token, process.env.JWT_SECRET);

        ctx.state.user = {
            _id: decoded._id,
            userId: decoded.userId,
        };
        _b87‍.g.console.log(decoded);

        return next();
    } catch ( e ) {
        return next();
    }
}

_b87‍.d(jwtMiddleware);